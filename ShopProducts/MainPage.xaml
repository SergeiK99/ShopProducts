<Page
    x:Class="ShopProducts.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ShopProducts"
    xmlns:vm="using:ShopProducts.ViewModels"
    xmlns:models="using:ShopProducts.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="12">
        <GridView ItemsSource="{Binding Products}"
                  IsItemClickEnabled="False"
                  SelectionMode="None">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="models:Product">
                    <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="8" Padding="8" Margin="8">
                        <StackPanel>
                            <Image Source="{Binding ImageSourceUri}" Height="100" Stretch="UniformToFill" />
                            <TextBlock Text="{Binding Name}" FontWeight="Bold" Margin="0,8,0,0" />
                            <TextBlock Text="{Binding FormattedPrice}" Foreground="Green"/>
                            <Button Content="Добавить в корзину" Margin="0,8,0,0" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </GridView.ItemTemplate>

            <GridView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid MaximumRowsOrColumns="4"/>
                </ItemsPanelTemplate>
            </GridView.ItemsPanel>
        </GridView>
    </Grid>
</Page>
